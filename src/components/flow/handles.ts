/**
 * handles.ts
 *
 * Constants for React Flow handle IDs used across nodes.
 * Centralising them avoids typos when referencing handles in edge logic.
 *
 * Convention:
 *   SOURCE_HANDLE  — the output connection point (bottom / right of node)
 *   TARGET_HANDLE  — the input connection point (top / left of node)
 */

export const SOURCE_HANDLE = 'source';
export const TARGET_HANDLE = 'target';
